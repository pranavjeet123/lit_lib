# 🏗️ Component Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     rating-component                         │
│                   (Custom Element)                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ extends
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      LitElement                              │
│               (Base Web Component Class)                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ provides
                            ▼
        ┌───────────────────┴───────────────────┐
        │                                       │
        ▼                                       ▼
┌──────────────────┐                  ┌──────────────────┐
│  Reactive Props  │                  │   Shadow DOM     │
│  & State Mgmt    │                  │   Encapsulation  │
└──────────────────┘                  └──────────────────┘
```

## Component Layers

```
┌──────────────────────────────────────────────────────────────┐
│                        Public API                             │
│  Properties: value, max-rating, size, readonly, show-label   │
│  Events: rating-change                                        │
│  Methods: Direct property access                              │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                    Component Logic Layer                      │
│  - Event handlers (click, hover, keyboard)                   │
│  - State management (@state for hover)                        │
│  - Value calculations                                         │
│  - Event dispatching                                          │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                      Render Layer                             │
│  - Template rendering (html`...`)                            │
│  - Dynamic class generation (classMap)                        │
│  - SVG star rendering                                         │
│  - Conditional rendering (nothing)                            │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                      Shadow DOM                               │
│  - Encapsulated DOM tree                                     │
│  - Isolated styles (static styles)                           │
│  - Event retargeting                                          │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                      Browser DOM                              │
│  - Rendered in page                                          │
│  - Accessible to users                                        │
│  - Inspectable in DevTools                                    │
└──────────────────────────────────────────────────────────────┘
```

## Data Flow

```
User Interaction → Event Handler → Update Properties → Lit Reactive Update
       ↓                                                          ↓
   Keyboard/Mouse                                           Re-render
       ↓                                                          ↓
   Event Capture ──────────────────────────────────→ Update Shadow DOM
       ↓                                                          ↓
   Custom Event ←──────────────────────────────────── Visual Feedback
       ↓
   Parent Component
```

## Property Reactivity Flow

```
┌─────────────────┐
│  Parent sets    │
│  value="3"      │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│  @property decorator    │
│  triggers update        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  Lit schedules update   │
│  (batched, async)       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  render() called        │
│  with new value         │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  Template updates       │
│  (efficient diff)       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  DOM updated            │
│  Stars filled           │
└─────────────────────────┘
```

## Event Flow

```
User clicks star #3
        │
        ▼
┌──────────────────┐
│  handleStarClick │
│  method called   │
└────────┬─────────┘
         │
         ▼
┌────────────────────────┐
│  Update value property │
│  this.value = 3        │
└────────┬───────────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌─────────────────┐  ┌──────────────────┐
│  Lit re-renders │  │  dispatchEvent   │
│  component      │  │  rating-change   │
└─────────────────┘  └────────┬─────────┘
                              │
                              ▼
                     ┌──────────────────┐
                     │  Event bubbles   │
                     │  to parent       │
                     └──────────────────┘
```

## Style Encapsulation

```
┌────────────────────────────────────────────────────────┐
│                     Host App                            │
│  <style>                                               │
│    .star { color: blue; }  ← Does NOT affect component │
│  </style>                                              │
│                                                        │
│  <rating-component>                                    │
│    │                                                   │
│    │  Shadow Boundary ─────────────────────────────┐  │
│    │                                                │  │
│    │  #shadow-root                                  │  │
│    │    <style>                                     │  │
│    │      .star { color: gold; }  ← Only here      │  │
│    │    </style>                                    │  │
│    │                                                │  │
│    │    <div class="star">⭐</div>                  │  │
│    │                                                │  │
│    └────────────────────────────────────────────────┘  │
│  </rating-component>                                   │
└────────────────────────────────────────────────────────┘
```

## TypeScript Type Safety

```
┌─────────────────────────────────────────────────────────┐
│              TypeScript Compiler                         │
│  - Checks property types at compile time                │
│  - Validates event detail structure                     │
│  - Ensures method signatures correct                    │
└────────────┬────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│              Generated JavaScript                        │
│  - Decorators transformed to descriptors                │
│  - Types stripped out                                   │
│  - ES modules for modern bundlers                       │
└────────────┬────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│              Type Definitions (.d.ts)                    │
│  - Exported for consumers                               │
│  - IDE autocomplete support                             │
│  - Type checking in other projects                      │
└─────────────────────────────────────────────────────────┘
```

## Build Process

```
Source Files
    │
    ├── rating-component.ts
    ├── rating-component.css
    └── index.html
    │
    ▼
TypeScript Compiler (tsc)
    │
    ├── Type checking
    ├── Generate .d.ts files
    └── Transpile to JS
    │
    ▼
Vite Build
    │
    ├── Bundle optimization
    ├── Tree shaking
    ├── Minification
    └── Source maps
    │
    ▼
Output (dist/)
    │
    ├── rating-component.js
    ├── rating-component.d.ts
    └── rating-component.js.map
    │
    ▼
NPM Package
```

## Usage Integration

```
┌────────────────────────────────────────────────────┐
│                  Framework Apps                     │
│                                                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │   React    │  │    Vue     │  │  Angular   │  │
│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  │
│        │                │                │         │
│        └────────────────┴────────────────┘         │
│                        │                           │
│                        ▼                           │
│         ┌──────────────────────────────┐          │
│         │    rating-component          │          │
│         │   (Standard Web Component)   │          │
│         └──────────────────────────────┘          │
│                        │                           │
│                        ▼                           │
│              Browser Native APIs                   │
│        (Custom Elements, Shadow DOM)               │
└────────────────────────────────────────────────────┘
```

## Accessibility Architecture

```
┌────────────────────────────────────────────────────┐
│              Component Structure                    │
│                                                     │
│  <div role="group" aria-label="Star rating">      │
│    │                                               │
│    └── <span role="radio"                         │
│              aria-checked="true/false"            │
│              aria-label="1 star"                  │
│              tabindex="0">                        │
│          <svg>...</svg>                           │
│        </span>                                     │
│                                                     │
│  Keyboard Events:                                  │
│    - Tab: Focus management                        │
│    - Arrow: Navigate between stars                │
│    - Enter/Space: Select rating                   │
│                                                     │
│  Screen Reader:                                    │
│    "Star rating, 3 stars out of 5 selected"      │
└────────────────────────────────────────────────────┘
```

## Performance Optimization

```
Lit's Reactive Update Cycle:

User Action
    │
    ▼
Property Change
    │
    ▼
Update Scheduled (requestUpdate)
    │
    ├── Batched updates
    └── Async (next microtask)
    │
    ▼
render() Called Once
    │
    ├── Template literal processing
    └── Virtual comparison
    │
    ▼
Efficient DOM Update
    │
    ├── Only changed nodes
    ├── Reuse existing elements
    └── Minimal layout thrashing
    │
    ▼
Browser Paint/Composite
```

## State Management

```
┌────────────────────────────────────────────────────┐
│              Component State                        │
│                                                     │
│  Public State (@property):                         │
│    - value                                         │
│    - maxRating                                     │
│    - readonly                                      │
│    - size                                          │
│    - showLabel                                     │
│                                                     │
│  Private State (@state):                           │
│    - hoverValue                                    │
│                                                     │
│  Derived State (computed in render):               │
│    - displayValue = hoverValue || value            │
│    - isStarFilled(index)                          │
│    - getStarClasses(index)                        │
└────────────────────────────────────────────────────┘
```

## Summary

This architecture demonstrates:

1. ✅ **Separation of Concerns**: Clear layers (API, Logic, Render, DOM)
2. ✅ **Encapsulation**: Shadow DOM isolates styles and DOM
3. ✅ **Reactivity**: Automatic updates via Lit's property system
4. ✅ **Type Safety**: TypeScript throughout
5. ✅ **Performance**: Efficient diffing and batched updates
6. ✅ **Accessibility**: ARIA, keyboard, semantic HTML
7. ✅ **Maintainability**: Clean, documented, testable code
8. ✅ **Reusability**: Framework-agnostic web component

---

Use this diagram during your interview to explain:
- How the component is structured
- Why you made certain decisions
- How data flows through the system
- How the component integrates with other frameworks
